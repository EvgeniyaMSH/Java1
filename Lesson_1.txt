public class Zadacha1 {
    public static void main(String[] args) {
    }

    /**
     * —оздать переменные всех пройденных типов данных, и инициализировать их значени€;
     */
    static void Zadcha2() {
        byte b = 123;
        short st = 12345;
        char ch = 'A';
        int integer = 24;
        long lg = 124345L;
        float fl = 11.123456f;
        double dl = 1234.12345;
        boolean bool = true;
    }

    /**
     * Ќаписать метод вычисл€ющий выражение a * (b + (c / d)) и возвращающий результат,
     * где a, b, c, d Ц входные параметры этого метода;
     */
    static double Calc(double a, double b, double c, double d) {
        return a * (b + (c / d));
    }

    /**
     * Ќаписать метод, принимающий на вход два числа, и провер€ющий что их сумма лежит в
     * пределах от 10 до 20(включительно), если да Ц вернуть true, в противном случае Ц false;
     */
    static boolean Chisla(int a, int b) {
        int sum = a + b;
        return sum > 9 && sum < 21;
    }

    /**
     * Ќаписать метод, которому в качестве параметра передаетс€ целое число, метод должен
     * напечатать в консоль положительное ли число передали, или отрицательное;
     * «амечание: ноль считаем положительным числом
     */
    static void printChislo(int num) {
        String word = "ѕоложительное";
        if (num < 0) word = "ќтрицательное";

        System.out.println(word);
    }

    /**
     * Ќаписать метод, которому в качестве параметра передаетс€ целое число, метод должен
     * вернуть true, если число отрицательное;
     */
    static boolean Chislominus(int num) {
        return num < 0;
    }

    /**
     * Ќаписать метод, которому в качестве параметра передаетс€ строка, обозначающа€ им€,
     * метод должен вывести в консоль сообщение Ђѕривет, указанное_им€!ї;
     */
    static void Hello(String name) {
        System.out.printf("ѕривет, %s!\n", name);
    }

    /**
     * Ќаписать метод, который определ€ет €вл€етс€ ли год високосным, и выводит сообщение в
     * консоль.  аждый 4-й год €вл€етс€ високосным, кроме каждого 100-го, при этом каждый 400-й Ц
     * високосный год.
     */
    static void Year(int year) {
        if (Yearone(year)) {
            System.out.println("¬исокосный");
        } else {
            System.out.println("Ќе високосный");
        }
    }

    private static boolean Yearone(int year) {
        if (((year % 4) == 0) && !((year % 100) == 0)) {
            return true;
        } else if ((year % 400) == 0) {
            return true;
        }
        return false;